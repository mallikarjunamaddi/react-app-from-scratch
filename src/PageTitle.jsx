import React from "react";

function PageTitle() {
    const title = "React Scratch App";
    return (
        <h1>{title}</h1> // {} helps us to mix up js in html and to render dynamic content.
    );
}

export default PageTitle;